----core----
-test on mobile and move the update tasks to a service worker if needed
-add mobile support
-task/day planned work brief and concise overview
-find a way to formalise productivity (possibly length * difficulty)

--stats
-add a weekly overview of productivity

--server
-add a more robust server and offline strategy
-add a sync all tasks button (could also sync hourly)
-possibly change to pushing changes to firebase every save interval

--prelaunch--
-add the option to skip signing in and instead use local storage
-add security, e.g. a user should only be able to access their own tasks (https://firebase.google.com/docs/firestore/security/rules-query) (https://firebase.google.com/docs/storage/security/index?authuser=0#authorization)
-add a user feedback form ("what would you change?")
-host it with a good domain name ( https://github.com/firebase/firebaseui-web-react )
-test on different devices and web broswers
-possibly refactor using react hooks ( https://reactjs.org/docs/hooks-intro.html)
-add a version num to the side bar
-tidy index.html
-setup donations (https://ko-fi.com/
-change notifications request ui/ux ( https://developers.google.com/web/fundamentals/push-notifications/permission-ux )
-improve the sign in ui ( https://github.com/firebase/firebaseui-web-react )

----bugs----
-add unit tests (https://jestjs.io/docs/en/tutorial-react)
-improve the timing and countdown, currently the timings are not in sync

----UI/UX----
-improve side bar ui and ux
-improve the moving task ux
-add an option to change focus from long to short tasks (change the task height) and to turn off objectives
-maybe add back the visual task percentage when clicked on

----non-essential----
-add dark mode
-add settings
-add in app purchase for dark mode

--ai--
-add ai to suggest the ordering of tasks - for optimal efficiency and to reduce burnout
-experiment with using a monte carlo tree search to order the tasks
-add ai autocomplete for task names and suggested duration
-create different user personaliity bots to train ai e.g. an organised bot, a procrastinator, a long distance, a sprinter
-create a more data driven application for ai, e.g. allow the ai to change break times, notification durations etc
-add tagging of tasks to enable fair comparison of metrics (e.g. you might compete a programming task quicker than a writing one), store tags on user